#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;

    if (b == c) cout << a << endl;
    if (a == c) cout << b << endl;
    if (a == b) cout << c << endl;
}
